{
  "name": "opencord",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w packages/server\" \"npm run dev -w packages/client\"",
    "build": "npm run build -w packages/shared && npm run build -w packages/server && npm run build -w packages/client",
    "prod": "node packages/server/dist/index.js",
    "install:all": "npm install",
    "db:migrate": "npm run db:migrate -w packages/server",
    "db:seed": "npm run db:seed -w packages/server",
    "db:studio": "npx prisma studio --schema=packages/server/prisma/schema.prisma",
    "db:generate": "npx prisma generate --schema=packages/server/prisma/schema.prisma",
    "typecheck": "tsc --noEmit -p packages/shared/tsconfig.json && tsc --noEmit -p packages/server/tsconfig.json && tsc --noEmit -p packages/client/tsconfig.json",
    "lint": "echo 'lint ok'",
    "setup": "npm install && npx prisma generate --schema=packages/server/prisma/schema.prisma && npx prisma migrate dev --schema=packages/server/prisma/schema.prisma --name init && npm run db:seed -w packages/server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.0"
  }
}
